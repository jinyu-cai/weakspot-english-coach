{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.6.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.6.0 --activate && pnpm build",
  "outputDirectory": ".next"
}
